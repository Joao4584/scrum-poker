{
  "name": "meta-scrum-poker",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "configs/*",
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "husky run pre-dev && turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint-config-prettier": "^9.1.0",
    "husky": "^8.0.0",
    "turbo": "^2.5.4"
  },
  "dependencies": {
    "@nestjs/typeorm": "^11.0.0",
    "pg": "^8.16.3",
    "typeorm": "^0.3.25"
  },
  "packageManager": "pnpm@8.10.5"
}
